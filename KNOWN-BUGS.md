# Dobra — Known Bugs

<!-- One section per confirmed bug. Number sequentially (B1, B2, …) — numbers permanent,
     never reused. Root-cause analysis stays here; resolved entries move to HISTORY.md.
     RULE (hook-enforced): a bug flips to FIXED only when a regression spec exists at
     tests/b<N>-*.py and passes. -->

*(none yet)*
